<div class="events-wrapper">
  <div class="container">
    <div class="center-inner">
			<div class="events">
				<div class="form-error">
					<% if @error_message %>
						<%= @error_message %>
					<% end %>
				</div>
        <div class="events-menu">
            <p class="menu-title">login</p>
        </div>
				<div class="events-display">
					<%= form_with model: @user, url:"/login" do |f| %>
						<div class="main-select">
							<div class="main-select">
								<div class="form-group">
									<%= link_to('',"/home/top", class:"fas fa-2x fa-arrow-left back-icon") %>
								</div>
								<div class="form-group">
									<%= f.email_field :email, class:"group-text",placeholder:"メールアドレス" %>
								</div>
								<div class="form-group">
									<%= f.password_field :password, class:"group-text",placeholder:"パスワード" %>
								</div>
								<div class="form-group">
									<%= f.submit value:"ログイン",class:"btn login" %>
								</div>
								<div class="form-group">
									<%= link_to("アカウントがない場合は作成する", "/signup", class: "btn signup") %>
								</div>
							</div>
						</div>
					<% end %>
        </div>
    	</div>
  	</div>
	</div>
</div>
